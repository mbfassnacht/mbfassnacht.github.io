var React = require('react');
var ReactDOM = require('react-dom');
var model = require('./projectsViewer-model');
var TweenMax = require('gsap');

var ProjectsViewer = React.createClass({

	render: function() {  

		return (
		  	<div className="projects-viewer" >
		
			</div>
		);
   	}
});

module.exports = ProjectsViewer;